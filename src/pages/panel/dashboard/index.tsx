export default function Dashboard() {
  // console.log(data, error);

  // const user = authContext((state) => state.user);

  // console.log(user);

  return (
    <div>
      <p>Aqui você pode ver seu perfil</p>
    </div>
  );
}
